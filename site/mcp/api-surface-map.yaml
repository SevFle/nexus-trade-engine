# MCP API surface map.
# GENERATED by scripts/generate_mcp_api_surface.py — do not hand-edit.
# Re-run it after changing the MCP surface (tools/resources/auth/errors).

meta:
  generated_at: '2026-07-16T07:18:51Z'
  generator: scripts/generate_mcp_api_surface.py
  source_of_truth:
  - engine/mcp/tool_definitions.py
  - engine/mcp/resources.py
  - engine/mcp/handlers.py
  - engine/mcp/errors.py
  - engine/mcp/auth.py
  note: This file is GENERATED — do not hand-edit. Re-run the generator after changing the MCP surface.
summary:
  tool_count: 11
  resource_count: 5
  transports:
  - stdio
  - http
  auth_methods:
  - jwt
  - api_key
  - anonymous
modules:
- file: engine/mcp/config.py
  responsibility: MCPServerSettings — every NEXUS_MCP_* knob. Separate from engine.config so the server
    can run as a standalone stdio process.
- file: engine/mcp/tool_definitions.py
  responsibility: 'Declarative tool catalog (single source of truth): name, description, JSON-Schema input_schema,
    RBAC required_role, MCP ToolAnnotations.'
- file: engine/mcp/handlers.py
  responsibility: dispatch_tool — routes a tools/call to its adapter, validates args against the schema,
    applies pagination, guards result size.
- file: engine/mcp/adapters/__init__.py
  responsibility: EngineServices (injectable capabilities) + PortfolioStore (in-memory portfolios, ownership
    model) + to_jsonable.
- file: engine/mcp/adapters/portfolio_adapter.py
  responsibility: 'Portfolio tools: status, positions, position, unrealized P&L, orders.'
- file: engine/mcp/adapters/strategy_adapter.py
  responsibility: 'Strategy tools: list_strategies, get_strategy_details.'
- file: engine/mcp/adapters/market_data_adapter.py
  responsibility: 'Market tools: get_market_data, get_cost_model, get_performance_metrics.'
- file: engine/mcp/adapters/backtest_adapter.py
  responsibility: run_backtest — historical strategy backtest.
- file: engine/mcp/resources.py
  responsibility: Static reference resources (strategy catalog, symbols, timeframes, risk ranges, cost-model
    defaults).
- file: engine/mcp/auth.py
  responsibility: extract_principal + require_role — resolves a credential to an AuthPrincipal and enforces
    the shared ROLE_HIERARCHY.
- file: engine/mcp/rate_limiter.py
  responsibility: Per-principal in-memory token bucket.
- file: engine/mcp/pagination.py
  responsibility: Cursor pagination (base64 offset) for list-heavy tools + ResultGuard (token-budget trimming).
- file: engine/mcp/progress.py
  responsibility: ProgressReporter wrapping session.send_progress_notification (no-op-safe).
- file: engine/mcp/errors.py
  responsibility: MCPError hierarchy with JSON-RPC codes; map_engine_exception normalises engine errors
    so tracebacks never leak.
- file: engine/mcp/observability.py
  responsibility: structlog events + counters/histograms via the pluggable MetricsBackend.
tools:
- name: get_cost_model
  required_role: viewer
  read_only: true
  destructive: false
  idempotent: true
  paginated:
    cursor_enabled: false
    list_key: null
  required_fields:
  - symbol
  - quantity
  - price
  properties:
  - avg_volume
  - price
  - quantity
  - side
  - symbol
  description: Estimate the transaction cost breakdown for a hypothetical trade (commission, spread, slippage,
    exchange fee, tax) using the engine cost model. Useful for cost-aware strategy decisions. Read-only.
- name: get_market_data
  required_role: viewer
  read_only: true
  destructive: false
  idempotent: true
  paginated:
    cursor_enabled: true
    list_key: bars
  required_fields:
  - symbol
  properties:
  - cursor
  - interval
  - limit
  - period
  - symbol
  description: Fetch OHLCV market data (bars) for a symbol over a period. Returns timestamp, open, high,
    low, close, volume per bar. Results are paginated. Read-only.
- name: get_orders
  required_role: viewer
  read_only: true
  destructive: false
  idempotent: true
  paginated:
    cursor_enabled: true
    list_key: orders
  required_fields: []
  properties:
  - cursor
  - limit
  - portfolio_id
  description: Return the trade/order history for a portfolio (chronological). Results are paginated to
    stay within the assistant's context budget. Read-only.
- name: get_performance_metrics
  required_role: viewer
  read_only: true
  destructive: false
  idempotent: true
  paginated:
    cursor_enabled: false
    list_key: null
  required_fields:
  - equity_curve
  properties:
  - equity_curve
  - initial_capital
  description: Compute performance metrics (total return, annualized return, Sharpe, Sortino, max drawdown,
    win rate, profit factor) from an equity curve and optional trade log. Read-only.
- name: get_portfolio_status
  required_role: viewer
  read_only: true
  destructive: false
  idempotent: true
  paginated:
    cursor_enabled: false
    list_key: null
  required_fields: []
  properties:
  - portfolio_id
  description: 'Return a summary of a portfolio: cash, total market value, total return percentage, and
    realized P&L. Read-only.'
- name: get_position
  required_role: viewer
  read_only: true
  destructive: false
  idempotent: true
  paginated:
    cursor_enabled: false
    list_key: null
  required_fields:
  - symbol
  properties:
  - portfolio_id
  - symbol
  description: Look up a single open position by symbol. Returns quantity, average cost, current price,
    market value, cost basis, unrealized P&L (absolute and percentage), and portfolio allocation weight.
    Raises a not-found error when the symbol is not held. Read-only.
- name: get_positions
  required_role: viewer
  read_only: true
  destructive: false
  idempotent: true
  paginated:
    cursor_enabled: false
    list_key: null
  required_fields: []
  properties:
  - portfolio_id
  description: List open positions in a portfolio with quantity, average cost, current price, market value,
    cost basis, unrealized P&L (absolute and percentage), and allocation weight. Read-only.
- name: get_strategy_details
  required_role: viewer
  read_only: true
  destructive: false
  idempotent: true
  paginated:
    cursor_enabled: false
    list_key: null
  required_fields:
  - strategy_name
  properties:
  - strategy_name
  description: 'Return full metadata for a single strategy: description, version, author, default parameters,
    symbols, and timeframe. Read-only.'
- name: get_unrealized_pnl
  required_role: viewer
  read_only: true
  destructive: false
  idempotent: true
  paginated:
    cursor_enabled: false
    list_key: null
  required_fields: []
  properties:
  - portfolio_id
  description: Aggregate the open (unrealized) profit & loss across a portfolio. Returns the net total
    unrealized P&L in absolute and percentage terms, plus a per-position breakdown. Useful for a quick
    'how am I doing right now' snapshot. Read-only.
- name: list_strategies
  required_role: viewer
  read_only: true
  destructive: false
  idempotent: true
  paginated:
    cursor_enabled: true
    list_key: strategies
  required_fields: []
  properties: []
  description: Enumerate all installed trading strategies with their version, description, author, supported
    symbols, and default parameters. Read-only.
- name: run_backtest
  required_role: quant_dev
  read_only: true
  destructive: false
  idempotent: true
  paginated:
    cursor_enabled: false
    list_key: null
  required_fields:
  - strategy_name
  - symbol
  - start_date
  - end_date
  properties:
  - end_date
  - initial_capital
  - start_date
  - strategy_name
  - symbol
  description: Run a historical backtest of a strategy on a single symbol over a date range. Returns performance
    metrics (total return, Sharpe, max drawdown, win rate, cost drag), trade count, and final capital.
    This is compute-only and never places live orders. Requires the 'quant_dev' role.
resources:
- uri: nexus://cost-model/defaults
  name: Cost Model Defaults
  mime_type: application/json
  description: Default transaction-cost-model parameters used by the engine.
- uri: nexus://risk-parameters/ranges
  name: Risk Parameter Ranges
  mime_type: application/json
  description: Configurable risk parameter bounds (min/max/default).
- uri: nexus://strategies/catalog
  name: Strategy Catalog
  mime_type: application/json
  description: Catalog of installed strategies with parameters and metadata.
- uri: nexus://symbols/list
  name: Symbol Universe
  mime_type: application/json
  description: Commonly traded symbols available for backtesting and market data.
- uri: nexus://timeframes/list
  name: Supported Timeframes
  mime_type: application/json
  description: Bar intervals accepted by market-data and backtest tools.
auth:
  principal:
    fields:
    - user_id
    - role
    - email
    - scopes
    - auth_method
  methods:
  - name: jwt
    description: Engine JWT, decoded with the exact validator the REST API uses (decode_token).
    transport: meta.authorization or NEXUS_MCP_TOKEN
  - name: api_key
    description: Static API-key table (NEXUS_MCP_STATIC_API_KEYS) for DB-free service-to-service auth.
    transport: meta.api_key or NEXUS_MCP_TOKEN
  - name: anonymous
    description: Issued only when NEXUS_MCP_AUTH_REQUIRED=false; gets NEXUS_MCP_DEFAULT_ROLE. Disables
      portfolio reads (ownership model rejects anonymous principals).
    transport: n/a
  role_hierarchy:
    viewer: 0
    user: 1
    retail_trader: 2
    quant_dev: 3
    developer: 4
    portfolio_manager: 5
    admin: 6
transports:
- name: stdio
  description: 'Canonical MCP local-server mode: the client spawns the server as a subprocess and talks
    JSON-RPC over stdin/stdout.'
  settings: transport=stdio
- name: http
  description: Long-lived, remotely-addressable server over HTTP+JSON-RPC.
  settings: http_host, http_port, http_path, http_log_level
errors:
- name: PARSE_ERROR
  code: -32700
- name: INTERNAL_ERROR
  code: -32603
- name: INVALID_PARAMS
  code: -32602
- name: METHOD_NOT_FOUND
  code: -32601
- name: INVALID_REQUEST
  code: -32600
- name: NOT_FOUND_ERROR
  code: -32005
- name: ENGINE_ERROR
  code: -32004
- name: RATE_LIMIT_ERROR
  code: -32003
- name: AUTHORIZATION_ERROR
  code: -32002
- name: AUTHENTICATION_ERROR
  code: -32001
